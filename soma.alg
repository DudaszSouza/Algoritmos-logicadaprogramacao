Algoritmo "Soma"

Var

   X, Y, soma : inteiro
   

Inicio

   escreva("Digite o valor de X: ")
   leia(x)
   escreva("Digite o valor de y: ")
   leia(y)
                  
   soma <- x + y
                  
   escreval("A soma de X + Y é igua a: ", soma)

Fimalgoritmo